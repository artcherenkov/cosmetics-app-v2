import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  labelStyle: {
    color: `red`,
  },
  button: {
    borderWidth: 1,
    borderColor: `red`,
    marginTop: 20,
  },
});
