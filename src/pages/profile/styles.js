import { StyleSheet } from "react-native";

export default StyleSheet.create({
  previewContainer: {
    width: `100%`,
    height: `100%`,
    justifyContent: `center`,
    alignItems: `center`,
  },
  previewHeader: {
    fontSize: 48,
  },
});
